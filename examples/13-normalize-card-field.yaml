id: normalize-card-field
name: Normalize card field name

log:
  match:
    attributes.cc_last4: exists
  transform:
    rename:
      - from: attributes.cc_last4
        to: attributes.card_last_four
