id: redact-cvv
name: Redact CVV codes

log:
  match:
    - log_attribute: ["cvv"]
      exists: true
  transform:
    redact:
      - log_attribute: ["cvv"]
        regex: '^[0-9]{3,4}$'
        replacement: "[REDACTED]"
