id: redact-pan
name: Redact primary account numbers

log:
  match:
    - log_attribute: ["pan"]
      exists: true
  transform:
    redact:
      - log_attribute: ["pan"]
        regex: '^([0-9]{9,15})([0-9]{4})$'
        replacement: "[REDACTED]$2"
