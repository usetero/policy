id: checkout-api-drop-cart-item-added
name: Drop cart item added logs

log:
  match:
    - field:
        log_attribute: resource.service.name
      exact: checkout-api
    - field:
        log_field: body
      regex: "item added to cart"
  keep:
    strategy: none
